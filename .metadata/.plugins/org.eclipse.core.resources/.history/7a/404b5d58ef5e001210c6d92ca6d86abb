template <class Item>

class MinSTACK{
private:
	Item *s, *m;
	int top;

public:
	MinSTACK(int maxN){
		s = new Item(maxN);
		m = new Item(maxN);
		top = 0;
	}

	int empty() const{
		return top == 0;
	}

	void push(Item x){
		int min = top > 0 ? m[top ++]:MIN;
	}
};
