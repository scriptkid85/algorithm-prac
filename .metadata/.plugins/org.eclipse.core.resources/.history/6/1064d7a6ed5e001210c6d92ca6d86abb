template <class Item>

class TriSTACK{
private:
	Item *s;
	int top[3];

public:
	TriSTACK(int maxN){
		s = new Item(3 * maxN);
		top[0] = 0;
		top[1] = maxN;
		top[2] = 2 * maxN;
	}
	push(int stacknum, Item x){
		if(t)
	}

};
